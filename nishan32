<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VIP CRACK SYSTEM</title>
    <style>
        /* সাইটের ব্যাকগ্রাউন্ড এবং লুক ঠিক করার জন্য CSS */
        body {
            background-color: black;
            color: #00FF00; /* হ্যাকিং স্টাইল সবুজ রঙ */
            font-family: 'Courier New', Courier, monospace;
            text-align: center;
            margin: 0;
            overflow: hidden;
        }
        iframe {
            width: 100%;
            height: 100vh;
            border: none;
            filter: brightness(0.4); /* গেম সাইটকে হালকা অন্ধকার দেখাবে */
        }
        .overlay {
            position: absolute;
            top: 10%;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(0, 0, 0, 0.8);
            padding: 20px;
            border: 2px solid red;
            border-radius: 10px;
            z-index: 100;
        }
        .prediction {
            font-size: 30px;
            font-weight: bold;
            color: red;
            text-shadow: 0 0 10px red;
        }
    </style>
</head>
<body>

    <iframe src="https://www.hgzy32.com"></iframe>

    <div class="overlay">
        <h3>NISHAN VIP PREDICTOR</h3>
        <div id="period">PERIOD: Loading...</div>
        <div class="prediction" id="result">ANALYZING...</div>
        <div id="timer">00:00</div>
    </div>

    <script>
        // প্রেডিকশন লজিক এবং টাইমার সেট করার স্ক্রিপ্ট
        setInterval(() => {
            const now = new Date();
            const seconds = now.getSeconds();
            const wait = 60 - seconds;
            document.getElementById('timer').innerText = "WAIT: " + wait + "s";

            if (seconds === 0) {
                const res = Math.random() > 0.5 ? "BIG" : "SMALL";
                document.getElementById('result').innerText = res;
            }
        }, 1000);
    </script>
</body>
</html>
